projection;
strict ( 2 );
use draft;

define behavior for ZC_LEAVE_REQ_P alias LeaveRequest
{
  use create;
  use update;
  use delete;

  use action Approve;
  use action Reject;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}
